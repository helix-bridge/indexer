{
  "name": "Bridge",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ cBridge/heco",
    "create-heco-local": "cp subgraph.yaml.heco subgraph.yaml; graph create --node http://localhost:8020/ cBridge/heco",
    "remove-heco-local": "cp subgraph.yaml.heco subgraph.yaml; graph remove --node http://localhost:8020/ cBridge/heco",
    "deploy-heco-local": "cp subgraph.yaml.heco subgraph.yaml; graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cBridge/heco",
    "create-crab-local": "cp subgraph.yaml.crab subgraph.yaml; graph create --node http://localhost:8020/ cBridge/crab",
    "remove-crab-local": "cp subgraph.yaml.crab subgraph.yaml; graph remove --node http://localhost:8020/ cBridge/crab",
    "deploy-crab-local": "cp subgraph.yaml.crab subgraph.yaml; graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cBridge/crab"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
